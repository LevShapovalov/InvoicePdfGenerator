@model string
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <title>Счёт сформирован</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
        body {
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            background: radial-gradient(circle at top, #f0f4ff 0, #e5ecff 40%, #f9fafb 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
        }

        .page-wrapper {
            padding: 20px;
            width: 100%;
            max-width: 480px;
        }

        .status-card {
            background: #ffffff;
            border-radius: 20px;
            box-shadow: 0 18px 45px rgba(15, 23, 42, 0.12);
            padding: 28px 26px 24px;
            text-align: center;
        }

        .status-icon {
            width: 54px;
            height: 54px;
            border-radius: 999px;
            margin: 0 auto 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #ecfdf5;
            color: #16a34a;
            font-size: 28px;
        }

        .status-title {
            font-size: 22px;
            font-weight: 700;
            margin-bottom: 4px;
            color: #111827;
        }

        .status-text {
            font-size: 14px;
            color: #6b7280;
            margin-bottom: 20px;
        }

        .actions {
            display: flex;
            flex-direction: column;
            gap: 10px;
            margin-top: 4px;
        }

        .download-button {
            width: 100%;
            border: none;
            border-radius: 999px;
            padding: 10px 18px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            background: linear-gradient(135deg, #4f46e5, #6366f1);
            color: #ffffff;
            box-shadow: 0 10px 24px rgba(79, 70, 229, 0.35);
            transition: transform .12s ease, box-shadow .12s ease, filter .12s ease;
        }

        .download-button:hover {
            transform: translateY(-1px);
            box-shadow: 0 14px 30px rgba(79, 70, 229, 0.4);
            filter: brightness(1.03);
        }

        .download-button:active {
            transform: translateY(0);
            box-shadow: 0 8px 18px rgba(79, 70, 229, 0.3);
        }

        .back-link {
            display: inline-flex;
            justify-content: center;
            align-items: center;
            gap: 6px;
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
            color: #4b5563;
            padding: 8px 10px;
            border-radius: 999px;
            background: #f9fafb;
            border: 1px solid #e5e7eb;
            transition: background-color .12s ease, border-color .12s ease, color .12s ease;
        }

        .back-link:hover {
            background-color: #f3f4f6;
            border-color: #d1d5db;
            color: #111827;
        }
    </style>
</head>
<body>
    <div class="page-wrapper">
        <div class="status-card">
            <div class="status-icon">✓</div>
            <h1 class="status-title">Счёт готов</h1>
            <p class="status-text">
                PDF-файл успешно сформирован. Вы можете скачать его сейчас или вернуться к заполнению новых счетов.
            </p>

            <div class="actions">
                <form asp-controller="Home" asp-action="DownloadFile" method="post">
                    <input type="text" name="path" value="@Model" hidden />
                    <button class="download-button" type="submit">Скачать файл</button>
                </form>

                <a asp-action="DoPayment" asp-controller="Home" class="back-link">
                    ← Вернуться на главную
                </a>
            </div>
        </div>
    </div>
</body>
</html>
